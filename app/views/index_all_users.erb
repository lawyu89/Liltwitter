<div class="index-all-users">
  <h1>Who else is out there?</h1>

  <% @all_users.each do |user| %>
    <div>
      <a href="/users/<%= @user.id %>/profiles/<%= user.id %>"><%= user.username %></a>
    </div>
  <% end %>
</div>

